version: '3.3'
services:
  cert_checker:
    image: ghcr.io/lilkidsuave/cosmos-cert-extractor:latest
    environment:
      - CHECK_INTERVAL=0
      - WATCHDOG_ENABLED=true
    volumes:
      - CosmosDirectory:/input:ro
      - AdguardDirectory:/output/certs
